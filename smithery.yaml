# Smithery configuration for security-scanner-mcp
# https://smithery.ai/docs/config

# For local/stdio distribution (Claude Desktop, Claude Code)
startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
    required: []
  commandFunction: |
    (config) => ({
      command: 'npx',
      args: ['-y', 'security-scanner-mcp']
    })

# Build configuration
build:
  dockerfile: false
